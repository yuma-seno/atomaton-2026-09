diff --git a/docs/operations.md b/docs/operations.md
index eec3ac0..0f79303 100644
--- a/docs/operations.md
+++ b/docs/operations.md
@@ -355,10 +355,19 @@ job, and this deliberately does not duplicate it.
 `command`, so a check that started the servers a pull request declares would
 execute that pull request inside the job that decides whether it may merge. This
 one reads the pull request's `.github/atomaton/` as data and runs nothing under
-`--root`. The half that needs live servers — whether an allowlist pattern still
-names a tool that exists, whether a server starts at all — runs on the other end
-of the pipeline instead, in the release. See [Checks, deployment, and the jobs you
-will see](#checks-deployment-and-the-jobs-you-will-see).
+`--root`.
+
+That leaves one thing it cannot answer, because the answer only exists at runtime:
+whether a `tool_allowlist` pattern still names a tool its server actually offers,
+and whether a server starts at all. `atoma validate --with-live-tools` answers it —
+it starts each server a definition names and asks for `tools/list` — but it starts
+processes, so it cannot run on a pull request either.
+
+**Nothing delivered to you runs it.** It is available to wire into your own
+`deploy.atomaton_runs.targets` commands, on your default branch, where the servers
+started are the ones you have already merged rather than ones a pull request chose.
+This repository does exactly that for its own release, which is a worked example
+rather than a default you inherit.
 
 **Why this exists.** Atoma resolves every `mcp_servers` name against the tools
 file it is handed, and aborts before a single tool server starts if one is
@@ -409,13 +418,12 @@ and every branch, and no permission grants it. So a repository whose pipeline li
 in `config.yaml` is one an agent can set up, extend and repair; one whose pipeline
 lives in workflow YAML always needs a person.
 
-**A release starts the servers it would ship, and stops before publishing if they
-disagree with it.** `scripts/release.sh` runs `scripts/check-live-tools.sh` between
-building `dist/` and creating the release. That script starts every tool server
-the artifact declares and asks `atoma validate --with-live-tools` what each one
-actually advertises, which is what decides whether every `tool_allowlist` /
-`tool_denylist` pattern still names a tool that exists, whether two `unprefixed`
-servers claim one name, and whether a server starts at all.
+**This repository wires the live half into its own release; the template does not.**
+It is one worked example rather than something you inherit — nothing shipped runs
+it. Here, `scripts/release.sh` runs `scripts/check-live-tools.sh` between building
+`dist/` and creating the release, starting every server the artifact declares and
+holding what each one advertises against the guards and the names its configuration
+declares.
 
 It runs there — on the default branch, against `dist/` — and never on the pull
 request, for one reason: it starts processes, and `tools.servers` lets a pull
@@ -424,6 +432,10 @@ request name any `command`. The pull request check reads a pull request's
 is not tradeable. What this costs is a guard that has stopped guarding being found
 after the merge that broke it rather than as a red check on its pull request.
 
+The same shape works for you, against your own default branch and in your own
+`deploy.atomaton_runs.targets`: start the servers your merged configuration
+declares, before the step that publishes.
+
 ## What a tool can and cannot be protected from
 
 Every tool server runs as **one dedicated OS user with no sudo**. One user, so no
diff --git a/src/atomaton-runtime/tools/defaults.yaml b/src/atomaton-runtime/tools/defaults.yaml
index 832c6b9..97671ef 100644
--- a/src/atomaton-runtime/tools/defaults.yaml
+++ b/src/atomaton-runtime/tools/defaults.yaml
@@ -55,12 +55,11 @@ servers:
       # that write are the ones left out.
       #
       # A pattern here that matches nothing is reported at startup, in the run's log,
-      # so this list cannot quietly stop applying. It is checked against the live
-      # server before a release ships too -- `atoma validate --with-live-tools` asks
-      # this server what it offers and holds every pattern here to it. That runs
-      # after a merge, against the artifact being published, and not on a pull
-      # request: starting a pull request's declared servers is not something a
-      # check that reads its configuration as data may do.
+      # so this list cannot quietly stop applying. `atoma validate --with-live-tools`
+      # can answer it before a run instead: it starts this server and holds every
+      # pattern here to the tools the server actually offers. Nothing runs it for
+      # you -- it starts processes, so where it may run is a decision about your
+      # pipeline rather than a default.
       tool_allowlist:
         - read
         - grep
diff --git a/tests/contract/live-tools-check.test.ts b/tests/contract/live-tools-check.test.ts
index 6b4f48a..d6b3eff 100644
--- a/tests/contract/live-tools-check.test.ts
+++ b/tests/contract/live-tools-check.test.ts
@@ -29,7 +29,7 @@
  * is pinned here rather than left to whoever next edits the script.
  */
 import { describe, expect, test } from "bun:test";
-import { existsSync, readFileSync, statSync } from "node:fs";
+import { existsSync, readdirSync, readFileSync, statSync } from "node:fs";
 
 const CHECK = "scripts/check-live-tools.sh";
 const RELEASE = "scripts/release.sh";
@@ -179,4 +179,117 @@ describe("where it is wired", () => {
       ).toBe(false);
     }
   });
+
+  /**
+   * And the wiring is this repository's own, which is what the adopter-facing
+   * documentation now says.
+   *
+   * `docs/operations.md` tells an adopter in the second person that no shipped file
+   * references this check, so their release does not run it. That sentence is true
+   * only while nothing under `src/` or `self/` names it — `src/` is what
+   * `build-dist.ts` copies into `dist/`, and `self/` is this repository's own
+   * overlay. If someone wires it into the shipped template, this test fails and the
+   * documentation has to be corrected in the same change rather than quietly
+   * becoming false for every reader of the delivered docs.
+   *
+   * `src/atomaton-runtime/tools/defaults.yaml` is the sharp case, because it ships
+   * and an adopter is told to read it: it may mention `--with-live-tools` as
+   * something a project can wire, and must not claim the check runs for them.
+   */
+  test("nothing shipped references the check, which is what the docs promise", () => {
+    const referencing = [...walk("src"), ...walk("self")].filter((path) =>
+      body(path).includes("check-live-tools.sh"),
+    );
+    expect(
+      referencing,
+      "the delivered template now references the live tool check. `docs/operations.md` tells an " +
+        "adopter their release does not run it unless they wire it themselves; either that " +
+        "documentation must change with this, or the reference does not belong in a shipped tree.",
+    ).toEqual([]);
+  });
+
+  /**
+   * And the shipped file that DOES mention the flag says who runs it.
+   *
+   * `src/atomaton-runtime/tools/defaults.yaml` is copied into `dist/` and carried by
+   * the release zip, and `.github/atomaton/README.md` tells an adopter it is there to
+   * be read. Its `files_readonly` comment mentions `--with-live-tools`, which is
+   * fine as something a project can wire and false as a property of their release:
+   * the only caller is this repository's `scripts/release.sh`, and `scripts/` is not
+   * in the archive. So the mention has to be accompanied by the part that is true
+   * for that reader — that nothing runs it for them.
+   */
+  test("the shipped comment about the flag does not claim a check the reader has", () => {
+    const shipped = body("src/atomaton-runtime/tools/defaults.yaml");
+    expect(shipped, "the shipped defaults no longer mention the flag; drop this test").toContain("--with-live-tools");
+
+    // Comment markers and wrapping stripped, so the claim is checked as the prose a
+    // reader sees rather than as lines it happens to be broken across.
+    const prose = shipped
+      .replace(/^\s*#\s?/gm, "")
+      .replace(/\s+/g, " ");
+
+    expect(
+      prose,
+      "src/atomaton-runtime/tools/defaults.yaml ships to every adopter, and the only caller of " +
+        "--with-live-tools is this repository's scripts/release.sh, which is not in the archive. " +
+        "The comment may offer the flag as something a project can wire, and must say that nothing " +
+        "runs it for that reader.",
+    ).toMatch(/\b(?:nothing|nobody|no one)\s+(?:runs|invokes|executes)\s+it\b/i);
+
+    expect(
+      prose,
+      "the shipped comment now describes this repository's release wiring as if it were the " +
+        "reader's. `scripts/` is not in the release archive, so an adopter has no such check.",
+    ).not.toMatch(/before a release ships|artifact being published|release\.sh/);
+  });
+
+  /**
+   * And the adopter-facing page says the same thing about the reader's release.
+   *
+   * `docs/` is written to an adopter, and the page that describes "the jobs you will
+   * see" told them the live half "runs … in the release" — as if it were coverage
+   * they had. It is not: `src/atomaton/config.yaml` ships `targets: []`, no shipped
+   * file names the check, and the file names such a paragraph uses (`scripts/`,
+   * `dist/`) do not exist in an adopter's tree. This is the same class of failure
+   * `config-contract.test.ts` exists for — a page that fails a reader for following
+   * it — one step earlier: a page that lets them believe a check is running.
+   */
+  test("the adopter-facing page does not claim the reader's release runs it", () => {
+    const prose = body("docs/operations.md").replace(/\s+/g, " ");
+    expect(prose, "the page no longer mentions the check; drop this test").toContain("--with-live-tools");
+    expect(
+      prose,
+      "docs/operations.md is written to an adopter. It may offer the flag as something they can " +
+        "wire, and must say that nothing delivered runs it — the shipped config has `targets: []` " +
+        "and no shipped file references scripts/check-live-tools.sh.",
+    ).toMatch(/\b(?:nothing delivered|no shipped file|the template does not|shipped pipeline does not)\b/i);
+    // And the two phrasings that were wrong, so that adding the sentence above while
+    // leaving the claim standing is still a failure: "in the release" and "a release
+    // starts the servers it would ship" both read as coverage the adopter has.
+    expect(
+      prose,
+      "the page describes the live half as running in the reader's release. It runs in THIS " +
+        "repository's release, from scripts/release.sh, which is not in the archive an adopter " +
+        "receives — say so rather than leaving the claim unqualified.",
+    ).not.toMatch(/in the release|a release starts the servers it would ship/i);
+  });
 });
+
+/**
+ * Every text file under a directory, so a claim about "nothing shipped references
+ * this" is checked rather than asserted.
+ */
+function walk(dir: string): string[] {
+  if (!existsSync(dir)) return [];
+  const out: string[] = [];
+  for (const entry of readdirSync(dir, { withFileTypes: true })) {
+    const path = `${dir}/${entry.name}`;
+    if (entry.isDirectory()) {
+      out.push(...walk(path));
+    } else if (entry.isFile() && /\.(ts|tsx|js|mjs|sh|yaml|yml|md|json)$/.test(entry.name)) {
+      out.push(path);
+    }
+  }
+  return out;
+}
