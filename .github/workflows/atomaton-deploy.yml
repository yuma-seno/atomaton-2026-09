# AUTO-GENERATED by Atomaton -- DO NOT EDIT DIRECTLY.
# Generated from: src/workflows/atomaton-deploy.wac.ts
# Edits here are lost when this file is regenerated upstream.
#
# To change what CI or deployment DOES, you do not need this file: put the
# commands in .github/atomaton/config.yaml, under `checks` or `deploy`. That
# file is yours, it survives upgrades, and an
# agent can write it -- GitHub refuses to let one write a workflow.
# See docs/pipeline/overview.md, "The pipeline as commands".
#
# Upstream: https://github.com/yuma-seno/atomaton

name: Atomaton Deploy
'on':
  workflow_dispatch:
    inputs:
      target:
        description: Name of the deployment to run, as written under `deploy` in config.yaml. Leaving it empty deploys nothing.
        required: false
        type: string
        default: ''
      trigger:
        description: Leave this alone. Atomaton sets it when it starts this workflow itself.
        required: false
        type: string
        default: demand
  push:
    tags:
      - '**'
    branches:
      - '**'
concurrency:
  group: atomaton-deploy-${{ github.ref }}
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  plan-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      jobs: ${{ steps.plan.outputs.jobs }}
      tags_before: ${{ steps.plan.outputs.tags_before }}
    steps:
      - name: Checkout the default branch, which decides what may be deployed
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
      - name: Setup Bun
        with:
          bun-version: latest
        uses: oven-sh/setup-bun@v2
      - name: Read `deploy` and select what this run is for
        id: plan
        shell: bash
        env:
          ATOMATON_DEPLOY_REF: ${{ github.ref }}
          ATOMATON_DEPLOY_EVENT: ${{ github.event_name }}
          ATOMATON_DEPLOY_TRIGGER: ${{ inputs.trigger }}
          ATOMATON_DEPLOY_TARGET_INPUT: ${{ inputs.target }}
          ATOMATON_DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          ATOMATON_REPO: ${{ github.repository }}
          ATOMATON_PUSH_BEFORE: ${{ github.event.before }}
          GH_TOKEN: ${{ github.token }}
        run: |
          PLANNER="${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/plan_deploy.ts"
          if [ ! -f "$PLANNER" ]; then
            echo "::warning::$PLANNER is not on the default branch yet, so nothing could be planned and nothing was deployed. This is expected once, on the upgrade that adds it."
            echo "jobs=[]" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/plan_deploy.ts" --ref "${ATOMATON_DEPLOY_REF}" --default-branch "${ATOMATON_DEFAULT_BRANCH}" --event "${ATOMATON_DEPLOY_EVENT}" --trigger "${ATOMATON_DEPLOY_TRIGGER}" --target "${ATOMATON_DEPLOY_TARGET_INPUT}" --repo "${ATOMATON_REPO}" --before "${ATOMATON_PUSH_BEFORE}"
  deploy:
    name: deploy (${{ matrix.name }})
    runs-on: ${{ fromJSON(matrix.runs_on) }}
    timeout-minutes: 60
    permissions:
      contents: write
      id-token: write
    strategy:
      fail-fast: true
      max-parallel: 1
      matrix:
        include: ${{ fromJSON(needs.plan-deploy.outputs.jobs) }}
    if: needs.plan-deploy.outputs.jobs != '[]'
    needs:
      - plan-deploy
    steps:
      - name: Checkout the tree this deployment ships
        uses: actions/checkout@v4
        with:
          ref: ${{ matrix.ref }}
      - name: Setup Bun
        with:
          bun-version: latest
        uses: oven-sh/setup-bun@v2
      - name: Run configured environment setup
        shell: bash
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/run_environment_setup.ts"
      - name: Run this deployment's commands
        shell: bash
        env:
          ATOMATON_SECRET_0: ${{ secrets[matrix.secrets[0]] }}
          ATOMATON_SECRET_1: ${{ secrets[matrix.secrets[1]] }}
          ATOMATON_SECRET_2: ${{ secrets[matrix.secrets[2]] }}
          ATOMATON_SECRET_3: ${{ secrets[matrix.secrets[3]] }}
          ATOMATON_SECRET_4: ${{ secrets[matrix.secrets[4]] }}
          ATOMATON_SECRET_5: ${{ secrets[matrix.secrets[5]] }}
          ATOMATON_SECRET_6: ${{ secrets[matrix.secrets[6]] }}
          ATOMATON_SECRET_7: ${{ secrets[matrix.secrets[7]] }}
          ATOMATON_SECRET_8: ${{ secrets[matrix.secrets[8]] }}
          ATOMATON_SECRET_9: ${{ secrets[matrix.secrets[9]] }}
          ATOMATON_SECRET_NAMES: ${{ toJSON(matrix.secrets) }}
          ATOMATON_COMMANDS: ${{ toJSON(matrix.commands) }}
          ATOMATON_DEPLOY_TARGET: ${{ matrix.name }}
          GH_TOKEN: ${{ github.token }}
        run: |
          # Credentials arrive in numbered slots because this file is generated and cannot
          # know what a project called its Slack token; config.yaml does, and the names
          # travelled here beside them. Put the declared name back on each slot before
          # anything that needs one runs.
          ATOMATON_DECLARED_SECRETS="${ATOMATON_SECRET_NAMES:-[]}"
          ATOMATON_DECLARED_COUNT=$(echo "$ATOMATON_DECLARED_SECRETS" | jq -r 'length')
          slot=0
          while [ "$slot" -lt "$ATOMATON_DECLARED_COUNT" ]; do
            secret_name=$(echo "$ATOMATON_DECLARED_SECRETS" | jq -r ".[${slot}]")
            eval "secret_value=\${ATOMATON_SECRET_${slot}:-}"
            if [ -n "$secret_value" ]; then
              export "${secret_name}=${secret_value}"
              echo "Secret ${secret_name} is available to this run."
            else
              echo "::warning::config.yaml declares secret ${secret_name}, but this repository has no secret by that name. Whatever needs it will fail."
            fi
            slot=$((slot + 1))
          done

          echo "$ATOMATON_COMMANDS" | jq -r ".[]" | while IFS= read -r command; do
            echo "::group::$command"
            if ! bash -c "$command"; then
              echo "::endgroup::"
              echo "::error::${{ matrix.name }}: $command"
              exit 1
            fi
            echo "::endgroup::"
          done
  dispatch-new-tags:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      actions: write
    if: (needs.plan-deploy.outputs.tags_before != '') && (needs.deploy.result == 'success')
    needs:
      - plan-deploy
      - deploy
    steps:
      - name: Checkout the default branch, for the script
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
      - name: Setup Bun
        with:
          bun-version: latest
        uses: oven-sh/setup-bun@v2
      - name: Deploy any tag these deployments created
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          ATOMATON_REPO: ${{ github.repository }}
          ATOMATON_TAGS_BEFORE: ${{ needs.plan-deploy.outputs.tags_before }}
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/dispatch_new_tags.ts" --repo "${ATOMATON_REPO}"
