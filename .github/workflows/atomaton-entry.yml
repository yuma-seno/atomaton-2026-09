# AUTO-GENERATED by Atomaton -- DO NOT EDIT DIRECTLY.
# Generated from: src/workflows/atomaton-entry.wac.ts
# Edits here are lost when this file is regenerated upstream.
#
# To change what CI or deployment DOES, you do not need this file: put the
# commands in .github/atomaton/config.yaml, under `checks` or `deploy`. That
# file is yours, it survives upgrades, and an
# agent can write it -- GitHub refuses to let one write a workflow.
# See docs/pipeline/overview.md, "The pipeline as commands".
#
# Upstream: https://github.com/yuma-seno/atomaton

name: Atomaton Entry
'on':
  issues:
    types:
      - opened
permissions:
  actions: write
  issues: write
  pull-requests: write
  contents: write
  checks: read
jobs:
  route:
    runs-on: ubuntu-latest
    if: contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association)
    outputs:
      agent: ${{ steps.resolve.outputs.agent }}
      number: ${{ steps.resolve.outputs.number }}
      type: ${{ steps.resolve.outputs.type }}
      notify: ${{ steps.resolve.outputs.notify }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Bun
        with:
          bun-version: latest
        uses: oven-sh/setup-bun@v2
      - name: Resolve agent and context
        id: resolve
        shell: bash
        env:
          NUMBER: ${{ github.event.issue.number }}
          SENDER: ${{ github.event.sender.login }}
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/resolve_entry_agent.ts"
  run:
    if: needs.route.outputs.agent != ''
    uses: ./.github/workflows/atomaton-runner.yml
    with:
      agent: ${{ needs.route.outputs.agent }}
      number: ${{ needs.route.outputs.number }}
      type: ${{ needs.route.outputs.type }}
      notify: ${{ needs.route.outputs.notify }}
      session_mode: continue
    secrets: inherit
    needs:
      - route
