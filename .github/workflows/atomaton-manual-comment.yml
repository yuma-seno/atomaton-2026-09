# AUTO-GENERATED by Atomaton -- DO NOT EDIT DIRECTLY.
# Generated from: src/workflows/atomaton-manual-comment.wac.ts
# Edits here are lost when this file is regenerated upstream.
#
# To change what CI or deployment DOES, you do not need this file: put the
# commands in .github/atomaton/config.yaml under `checks.atomaton_runs.commands` or
# `deploy.atomaton_runs.targets`. That file is yours, it survives upgrades, and an
# agent can write it -- GitHub refuses to let one write a workflow.
# See docs/configuration.md, "The pipeline as commands".
#
# Upstream: https://github.com/yuma-seno/atomaton

name: Atomaton Manual Comment
'on':
  issue_comment:
    types:
      - created
permissions:
  actions: write
  issues: write
  pull-requests: write
  contents: write
  checks: read
jobs:
  parse:
    runs-on: ubuntu-latest
    if: (github.event.comment.user.type != 'Bot') || (github.event.comment.user.type == 'Bot' && contains(github.event.comment.body, 'atoma:dispatch'))
    outputs:
      agent: ${{ steps.dispatch.outputs.agent }}
      session_mode: ${{ steps.command.outputs.session_mode }}
      number: ${{ steps.target.outputs.number }}
      type: ${{ steps.target.outputs.type }}
      notify: ${{ steps.target.outputs.notify }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Bun
        with:
          bun-version: latest
        uses: oven-sh/setup-bun@v2
      - name: Parse slash command
        id: command
        if: |-
          (github.event.comment.user.type == 'Bot' &&
           contains(github.event.comment.body, 'atoma:dispatch')) ||
          (github.event.comment.user.type != 'Bot' &&
           contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association))
        shell: bash
        env:
          ATOMATON_COMMENT_BODY: ${{ github.event.comment.body }}
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/parse_comment_command.ts"
      - name: 'Guard: reject comment while atomaton/in-progress'
        id: guard
        if: (github.event.comment.user.type != 'Bot') && steps.command.outputs.control != 'stop'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          NUMBER: ${{ github.event.issue.number }}
          COMMENT_ID: ${{ github.event.comment.id }}
          COMMENTER: ${{ github.event.comment.user.login }}
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/guard_comment_during_run.ts" --number "${NUMBER}" --comment-id "${COMMENT_ID}" --commenter "${COMMENTER}"
      - name: Resolve target context
        id: target
        shell: bash
        env:
          NUMBER: ${{ github.event.issue.number }}
          IS_PR: ${{ toJSON(github.event.issue.pull_request != null) }}
          NOTIFY: ${{ github.event.comment.user.login }}
        run: |
          echo "number=${NUMBER}" >> "$GITHUB_OUTPUT"
          if [ "$IS_PR" = "true" ]; then
            echo "type=pr" >> "$GITHUB_OUTPUT"
          else
            echo "type=issue" >> "$GITHUB_OUTPUT"
          fi
          echo "notify=${NOTIFY}" >> "$GITHUB_OUTPUT"
      - name: Request a stop
        if: steps.command.outputs.control == 'stop'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          NUMBER: ${{ github.event.issue.number }}
          COMMENT_ID: ${{ github.event.comment.id }}
          COMMENTER: ${{ github.event.comment.user.login }}
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/request_stop.ts" --number "${NUMBER}" --comment-id "${COMMENT_ID}" --commenter "${COMMENTER}"
      - name: Resolve the agent to resume
        id: resume
        if: steps.command.outputs.control == 'resume'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          NUMBER: ${{ github.event.issue.number }}
        run: |
          bun run "${ATOMATON_MACHINERY_ROOT:-.}/.github/atomaton-runtime/scripts/resolve_resume_agent.ts" --number "${NUMBER}"
      - name: Decide what to dispatch
        id: dispatch
        shell: bash
        env:
          FROM_COMMAND: ${{ steps.command.outputs.agent }}
          FROM_RESUME: ${{ steps.resume.outputs.agent }}
          BLOCKED: ${{ steps.guard.outputs.blocked }}
        run: |
          if [ "$BLOCKED" = "true" ]; then
            AGENT=""
          else
            AGENT="${FROM_COMMAND:-${FROM_RESUME}}"
          fi
          echo "agent=${AGENT}" >> "$GITHUB_OUTPUT"
          echo "dispatching: ${AGENT:-nothing}"
      - name: Report invalid slash command
        if: steps.command.outputs.error != ''
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          NUMBER: ${{ steps.target.outputs.number }}
          ERROR: ${{ steps.command.outputs.error }}
        run: |
          gh issue comment "${NUMBER}" --body "Atomaton command error: ${ERROR}"
  run:
    if: needs.parse.outputs.agent != ''
    uses: ./.github/workflows/atomaton-runner.yml
    with:
      agent: ${{ needs.parse.outputs.agent }}
      number: ${{ needs.parse.outputs.number }}
      type: ${{ needs.parse.outputs.type }}
      notify: ${{ needs.parse.outputs.notify }}
      session_mode: ${{ needs.parse.outputs.session_mode }}
    secrets: inherit
    needs:
      - parse
