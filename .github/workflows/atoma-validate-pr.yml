# AUTO-GENERATED by Atoma Autonomous Delivery -- DO NOT EDIT DIRECTLY.
# Generated from: src/workflows/atoma-validate-pr.wac.ts
# Edits here are lost when this file is regenerated upstream.
#
# To change what CI or deployment DOES, you do not need this file: put the
# commands in .github/atoma/config.json under `checks.commands` or
# `deploy.targets`. That file is yours, it survives upgrades, and an agent can
# write it -- GitHub refuses to let one write a workflow.
# See docs/customization.md, "Set up CI and deployment".
#
# Upstream: https://github.com/yuma-seno/atomaton

name: Atoma Validate PR
'on':
  workflow_dispatch:
    inputs:
      number:
        description: Pull request number
        required: true
        type: string
      branch:
        description: Head branch of the pull request
        required: true
        type: string
      reviewer:
        description: Agent to dispatch when CI passes
        required: false
        type: string
        default: reviewer
      engineer:
        description: Agent to dispatch when CI fails
        required: false
        type: string
        default: engineer
permissions:
  actions: write
  issues: write
  pull-requests: write
  contents: write
  checks: write
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    concurrency:
      group: atoma-validate-${{ inputs.number }}
      cancel-in-progress: true
    permissions:
      actions: write
      issues: write
      pull-requests: write
      contents: write
      checks: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Checkout the pull request's own tree, as data
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          path: pr-head
      - name: Setup Bun
        with:
          bun-version: latest
        uses: oven-sh/setup-bun@v2
      - name: Install Atoma CLI
        shell: bash
        run: |
          VERSION="v0.1.30"
          if [ "$VERSION" = "source" ]; then
            echo "Building Atoma from source (atoma-src/) ..."
            cargo install --path atoma-src --force --locked
          elif [ "$VERSION" = "latest" ]; then
            URL="https://github.com/yuma-seno/atoma/releases/latest/download/atoma-linux-x86_64"
            echo "Downloading Atoma ${VERSION} ..."
            curl -fsSL "$URL" -o /usr/local/bin/atoma
            chmod +x /usr/local/bin/atoma
          else
            URL="https://github.com/yuma-seno/atoma/releases/download/${VERSION}/atoma-linux-x86_64"
            echo "Downloading Atoma ${VERSION} ..."
            curl -fsSL "$URL" -o /usr/local/bin/atoma
            chmod +x /usr/local/bin/atoma
          fi
          atoma --version
      - name: Validate the deliverable this pull request would merge
        shell: bash
        run: |
          STATUS=0
          bun run "${ATOMA_MACHINERY_ROOT:-.}/.github/scripts/validate_deliverable.ts" --root "pr-head" --report "${RUNNER_TEMP}/atoma-deliverable-problems.txt" || STATUS=$?
          if [ "$STATUS" -gt 1 ]; then
            echo "::error::the deliverable could not be validated; see the log above"
            exit 1
          fi
      - name: Read the CI workflow name from config
        id: cfg
        shell: bash
        run: |
          WORKFLOW=$(bun run "${ATOMA_MACHINERY_ROOT:-.}/.github/scripts/get_config_value.ts" "workflows.ci" "atoma-check.yml")
          echo "workflow=${WORKFLOW}" >> "$GITHUB_OUTPUT"
      - name: Run CI and write its result where the ruleset looks
        id: validate
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          bun run "${ATOMA_MACHINERY_ROOT:-.}/.github/scripts/validate_pull_request.ts" --repo "${{ github.repository }}" --number "${{ inputs.number }}" --branch "${{ inputs.branch }}" --workflow "${{ steps.cfg.outputs.workflow }}" --reviewer "${{ inputs.reviewer }}" --engineer "${{ inputs.engineer }}" --deliverable-report "${RUNNER_TEMP}/atoma-deliverable-problems.txt"
      - name: Dispatch the agent the result calls for
        if: steps.validate.outputs.next_agent != ''
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          AGENT: ${{ steps.validate.outputs.next_agent }}
          NUMBER: ${{ inputs.number }}
          SUMMARY: ${{ steps.validate.outputs.summary }}
        run: |
          gh workflow run atoma-runner.yml \
            --repo "${{ github.repository }}" \
            -f agent="$AGENT" \
            -f number="$NUMBER" \
            -f type=pr
          echo "Dispatched $AGENT for #$NUMBER: $SUMMARY"
